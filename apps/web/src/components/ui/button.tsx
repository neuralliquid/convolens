// Re-exported from @convolens/ui — see packages/ui/src/components/button.tsx.
// This file used to hold an independent, byte-identical copy of the same
// component; kept as a re-export so `@/components/ui/button` call sites
// don't need to change while apps/web actually consumes the design-sync'd
// package. See baton task 8ec9b31f.
export { Button, LoadingButton, buttonVariants } from "@convolens/ui"
