// Re-exported from @convolens/ui — see packages/ui/src/components/input.tsx.
// apps/web's local copy had diverged ahead of the package (icon
// positioning, aria-invalid, controlled-value sync, refined clear-button/
// error styling); those improvements were ported into the package first
// so this re-export is not a regression. See baton task 8ec9b31f.
export { Input, SearchInput } from "@convolens/ui"
